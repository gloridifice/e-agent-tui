## 1. Implementation

- [x] 1.1 Add the reusable command lexer/styled-text component; run focused tests for source preservation, executable/flag/operator roles, quoting/escaping, redirections, and multiline/incomplete input.
- [x] 1.2 Integrate structured and standalone Preview plus wide/narrow History summaries, preserving existing cache, wrapping/clipping, metrics, and exports; run focused integration/regression tests and update the affected architecture contract.
- [x] 1.3 Run focused history export and rendering-boundary regression checks, validate the change, and inspect the implementation/specification diff before CLI archive and main-spec validation.
