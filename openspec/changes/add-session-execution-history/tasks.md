## 1. Review the terminal prototype before production implementation

- [ ] 1.1 Create a standalone Python TUI history demo in `target/prototype` using the repository Ferra palette and simulated output-free records. Show a title-free time axis, parallel lanes, exact metrics and a scrollable record list; exercise j/k, half/full-page movement, q/Esc, resizing and terminal restoration in a PTY. Keep this disposable demo separate from Rust implementation.
- [ ] 1.2 Obtain the user's review of the terminal demo and approval to implement. Reconcile any requested presentation changes with these deltas before proceeding; creating this change or the demo does not authorize production edits.

## 2. Execution records and adapter persistence

- [ ] 2.1 Define neutral operation/record/metric/query values and the versioned JSONL codec contract without adding filesystem or provider JSON access to `e-tui`. Unify result line/truncation normalization with activity projection. Verify fixtures for unknown versus zero, empty/truncated output, read limits versus observed counts, and allowlisted summaries excluding output/patches/unknown arguments.
- [ ] 2.2 Add adapter-owned recording/query effects for both executables using backend-confirmed cwd/session identity, stable safe keys, run IDs, scoped `.e/.gitignore` entries and exclusive session writers. Use bounded ordered workers, explicit recording-health feedback and finite query watermarks. Verify temporary-directory cases for nested cwd, resume append, session replacement, deferred drafts, competing writers, write failure, partial tails, unsupported schemas, identity mismatch, and orderly flush; do not touch real user traces.
- [ ] 2.3 Capture Pi and DSH live tool/command/available model boundaries before UI batching, retaining backend timing where valid and otherwise monotonic ingress duration with provenance. Preserve call/turn/parent IDs and reconnect gaps; suppress snapshot/replay duplication and repeated terminal events. Verify fast same-batch tools, overlapping calls, clock changes, missing starts/ends, failure/cancellation, and bounded capture under transport bursts. Do not modify backend protocols or install recording extensions.
- [ ] 2.4 Enrich native resume projections by matching trace identity and call IDs, including page-split calls and folded file groups. Verify saved metrics match live activity rendering, create rows still suppress their existing metrics, no output is synthesized, old trace-less calls show unknown duration, and missing/late trace results cannot duplicate executions or contaminate a replacement session.

## 3. Command completion and history actions

- [ ] 3.1 Extend the central command declarations with fixed subcommand descriptors/default dispatch; reuse current candidate/description/ranking helpers rather than undoing the active completion refactor. Register history's four actions. Verify whitespace, partial completion, default show, invalid extra arguments, explicit-submit-only behavior, localization, atomic-block suppression, and unchanged dynamic model/effort completion and provider forwarding.
- [ ] 3.2 Wire session-scoped history queries through runtime ports and actions. Implement path insertion using guarded ordinary composer editing, full chronological text export and stable top-ten ranking over eligible measured operations, followed by existing clipboard feedback. Verify spaces/non-ASCII paths, edits made during a pending request, no automatic prompt/transcript insertion, deferred drafts, stale results, failed clipboard writes, long/full summaries, incomplete warnings, finite live snapshots, ties and fewer than ten eligible records.

## 4. Full-screen page lifecycle and terminal history rendering

- [ ] 4.1 Add a closed full-screen page owner and Screen-level composition branch, independent of Input Pages and Reading. Preserve canonical conversation/draft/anchor/inactive-view state; keep backend reduction alive and restore without an obsolete store clone. Verify no-event enter/exit equality, atomic blocks, background completion, resize, session switch, incoming approval/question, paste/submission suppression and stale-load cancellation using scoped state/controller tests.
- [ ] 4.2 Add a shared full-screen browse key scope to the declarative defaults/resolver with one-row, half/full-page and exit actions; route page wheel input and committed-screen selection without hidden transcript scrolling or separator capture. Verify remap/disable behavior, q/Esc only closing the page, effective hints, viewport clamping, tiny heights and existing Input Page/Reading bindings remaining unchanged.
- [ ] 4.3 Render the approved title-free time axis, operation legend, parallel lanes, short-operation markers and chronological metrics with existing theme roles and downward rendering layers. Provide loading/empty/error/incomplete states and bounded page reads/layout. Verify narrow/zero geometry, long/non-ASCII summaries, non-color status readability, overlap geometry, exact durations, independent scrolling and no hidden transcript rebuilds on stable page frames; compare a real Ferra PTY capture with the approved demo.

## 5. Integrated delivery

- [ ] 5.1 Update the README's public commands/keys and discoverable storage guidance, localized completion/help labels, effective binding reference and the client architecture's full-screen/persistence boundary. Document attached-client-only coverage and command-text sensitivity without mirroring private implementation details. Run focused adapter/runtime/completion/page/projection/architecture regressions, builds for both executables, workspace formatting and Clippy for this cross-cutting change; report unavailable PTY or runtime checks explicitly.
- [ ] 5.2 Validate this change strictly, confirm every implementation task and the user's visual gate are satisfied, then merge deltas and archive through the documented OpenSpec CLI. Preserve unrelated dirty-tree work and do not archive other active changes; validate the three resulting main capabilities before reporting production delivery complete.
