## 1. Make the dependency boundary trustworthy

- [x] 1.1 Capture the implementation baseline without overwriting existing dirty-tree work: run the scoped architecture, input/page, render/transcript, state, reveal, and controller regression groups relevant below; record existing failures separately. Capture release `timing_frames` and `timing_syntax` workload counters and timings using the existing performance methodology so later zero-cost claims have a comparable baseline.
- [x] 1.2 Repair `crates/e-dsh/tests/architecture.rs` dependency discovery for local module paths, grouped imports with aliases, and alias-qualified calls, resolving references to their owning modules. Add isolated positive/negative fixtures for the audit's missed `screen::` calls and grouped `as` imports, actual nested cycles, re-exports, test-only items/modules (including `#[path]`), and comments/string literals. Confirm scanner fixtures pass and expose the Main-pane back-edge without counting mere module declarations as dependencies; do not suppress discovered production edges to get a green graph.
- [x] 1.3 Move Main composition from `ui.rs` into `ui/pane/main`, relocate the transcript/composer/status/page Region implementations behind their owning paths, and move `InputPageRegions` into the page shell. Remove execution callbacks into the composition root and make moved implementation imports explicit while preserving supported entry points. Verify `cargo test -p e-dsh --test architecture`, focused Main/Preview rendering and selection/resize regressions, and both adapter builds. If the corrected scanner exposes unrelated adapter cycles, report them rather than silently broadening scope or excluding them.

## 2. Consolidate input and page primitives

- [x] 2.1 Extract completion ranking mechanics from `input.rs` and reuse the subsequence/rank primitive with `command_catalog`; keep catalog policy independent of composer state and avoid reverse dependencies through agent DTO re-exports. Represent candidate fill/description/source together where compatible with supported callers. Verify existing completion cases plus stable roster ordering, command alphabetical tie-breaking, mode-name versus model-name matching differences, duplicate provider/model IDs, catalog refresh, and suppression for atomic blocks and `//` prefixes.
- [x] 2.2 Share paste/image range insertion, overlap removal, and shifting through a concrete range value or statically instantiated block type, retaining separate storage and payload ownership. Verify focused input tests for mixed text/images/pastes, adjacent blocks, word deletion, cursor boundary mapping, history draft restore, and failed prompt restore; check that the abstraction adds no per-paste image payload storage or additional payload clones.
- [x] 2.3 Move reusable linear focus-node construction to `page_core`, taking existing items and an ID-producing closure without an additional intermediate ID collection. Reuse it in applicable pages while retaining Model cross-column edges and Settings category behavior. Verify empty/disabled rosters, stable identity across refresh/localization, question navigation, model marks, and effective browse/edit key mappings.

## 3. Unify width and bottom-layout policy

- [x] 3.1 In a behavior-fix step separate from file moves, consolidate text/styled-line clipping around shared grapheme-safe width boundaries. Add focused regressions for zero/tiny widths, exact-fit ASCII (`abc` at width 3), CJK, combining marks and ZWJ sequences spanning style boundaries, ellipsis budget/style preservation, and Preview terminal rows clipped without wrapping or ellipsis. Reuse existing buffers/ranges where possible; do not introduce an unconditional extra whole-line flatten/allocation merely to support an abstraction, and leave complete-source copy unchanged.
- [x] 3.2 Introduce one concrete bottom-layout plan for accessory eligibility, row allocation, and transcript-height measurement; use it in both Main drawing modes and runtime paging calculations. Preserve their different positioning and clipping rules. Verify measured versus rendered heights with wrapping/model hints, open pages, approvals and queues, small terminals, deferred-new drafts retaining old-session accessories, and history/page scrolling; preserve held-screen and separator-placeholder bypasses.
- [x] 3.3 Share ruled-chrome policy in a Component, RGB interpolation in a downward leaf, RuntimeState breathing through the existing owner, transcript RenderOptions construction, and DisplayItem spinner eligibility. Preserve public compatibility entry points, non-RGB fallback, rule geometry, materialization timing, and the status label's separate breathing treatment. Run existing focused animation, layout, and rendering regressions; do not add tests solely for theme styling.

## 4. Share fade bookkeeping without merging reveal lanes

- [x] 4.1 Extract fade groups and their independent deadline into a concrete shared owner used by transcript and Preview tracks. Keep transcript holdback/grapheme admission and Preview row/block admission separate. Verify existing reveal tests for coincident deadlines, delayed ticks advancing one batch, zero rate, finite/open-stream drain to idle, append-only Markdown growth, same-target revisions, resize, cached/Reading page fade, and fresh reasoning row pacing; confirm no additional transcript invalidation from Preview-only work.

## 5. Split cohesive implementation units

- [x] 5.1 Finish the `input` split into completion, editing/atomic-block, and history responsibilities, and extract Model/Effort/Resume behavior from `input_page` behind its existing closed page facade. Reuse the primitives from section 2, retain narrow visibility and adapter-facing paths, and rerun scoped input/page/controller tests, including the existing temporary-model-prefix regressions. Do not replace the closed page enum with a plugin or trait registry.
- [x] 5.2 Split `render` into inline styling, table, list, and code/fence helpers behind the current Markdown API. Preserve the single event-driven InlineBuilder, width/provenance semantics, stable render-unit allocation, fence fallback, and collapse behavior. Run scoped Markdown/syntax/provenance regressions and compare the release syntax workload against 1.1.
- [x] 5.3 Split the relocated transcript Region into surface-line projection, activity folding, cache updates, and viewport painting with one-way dependencies. Preserve hidden reasoning adjacency, completion-gated folding, reveal suffix splices, spinner patches, prepend anchors, Reading geometry, complete-source copy, and visible-window work. Run focused transcript/cache/Reading and committed-screen-selection regressions; compare frame workload counters with 1.1.
- [x] 5.4 Isolate legacy test DTOs and mirror helpers from runtime-state production responsibilities, retaining characterization assertions against the canonical store. Split `runtime/state/reduction` into assistant, tool, activity/lifecycle, and history units as warranted by the remaining production code. Verify snapshot/prepend, result-before-call enrichment, surface replacement, submission echoes, and draft/session restoration using existing state and controller tests; ensure no test-support path enters production dependencies.

## 6. Verify the integrated refactor

- [x] 6.1 Rerun the corrected architecture gate and scoped suites for every touched subsystem, and build both `dshe` and `pie`. Compare release frame/syntax measurements with 1.1 at the same fixture, dimensions, and environment: investigate extra allocations/clones, cache rebuilds, materialized rows, or latency regressions instead of weakening workload assertions. Record unavailable reference-environment measurements explicitly; do not claim the P95 target or zero-cost equivalence without evidence.
- [x] 6.2 Run `cargo fmt --all`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets`, and `openspec validate refactor-e-tui-audit-findings --type change --strict`. Confirm the diff contains only intended remediation and preserves pre-existing user work. Keep `skip_specs: true` only while requirements remain unchanged; any newly required public behavior/API decision must be agreed and specified before implementation continues. Archive this change through the normal CLI only after all scoped checks and tasks are complete; do not synchronize or archive unrelated active changes.
