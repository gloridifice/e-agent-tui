## 1. Review the terminal prototype before production implementation

- [x] 1.1 Create a standalone Python TUI history demo in `target/prototype` using the repository Ferra palette and simulated output-free records. Show a title-free time axis, parallel lanes, exact metrics and a scrollable record list; exercise j/k, half/full-page movement, q/Esc, resizing and terminal restoration in a PTY. Keep this disposable demo separate from Rust implementation. Delivered `history_tui.py` with `--self-test` and `--snapshot`; self-tests and Windows PTY navigation/color/resize/exit checks passed. Captured `history_tui.svg` and inspected a PNG rendered from committed terminal cells. These ignored local artifacts are review aids, not shipped application code.
- [x] 1.2 Obtain the user's review of the terminal demo and approval to implement. Approved after reviewing the scrolling overview, per-turn and Top 50 views, operation/elapsed colors, shell labels and semantic-theme proposal. The user explicitly authorized recording the theme design and starting production implementation.

## 2. Execution records and adapter persistence

- [x] 2.1 Define neutral operation/record/metric/query values and the versioned JSONL codec contract without adding filesystem or provider JSON access to `e-tui`. Unify result line/truncation normalization with activity projection. Verify fixtures for unknown versus zero, empty/truncated output, read limits versus observed counts, and allowlisted summaries excluding output/patches/unknown arguments. Added `execution_history` typed Serde records, reference-only summary extraction, measured-call ranking and shared observed-output counts used by live/staged activity and Preview metrics. Scoped record/JSON round-trip/normalization and projection tests passed; persistence, timing ingress and replay enrichment remain separate pending tasks.
- [x] 2.2 Add adapter-owned recording/query effects for both executables using backend-confirmed cwd/session identity, stable safe keys, run IDs, scoped `.e/.gitignore` entries and exclusive session writers. Use bounded ordered workers, explicit recording-health feedback and finite query watermarks. Verify temporary-directory cases for nested cwd, resume append, session replacement, deferred drafts, competing writers, write failure, partial tails, unsupported schemas, identity mismatch, and orderly flush; do not touch real user traces. Both adapters now own bounded exclusive writer/query workers with stable path-safe keys, preserved scoped ignore entries, fixed-watermark byte-cursor paging, health errors and orderly detach/flush. Temporary-directory tests cover nested cwd, no pre-attachment/deferred file, session switch, resume, competing locks, unavailable paths, malformed/partial records, identity/version checks and bounded stable pages.
- [x] 2.3 Capture Pi and DSH live tool/command/available model boundaries before UI batching, retaining backend timing where valid and otherwise monotonic ingress duration with provenance. Preserve call/turn/parent IDs and reconnect gaps; suppress snapshot/replay duplication and repeated terminal events. Verify fast same-batch tools, overlapping calls, clock changes, missing starts/ends, failure/cancellation, and bounded capture under transport bursts. Do not modify backend protocols or install recording extensions. A provider-neutral ingress capture reducer now emits independent start/finish, turn and explicit model-step records; adapters invoke it after wire normalization and before UI batching. Tests cover backend timing, monotonic fallback under wall-clock reversal, measured zero, missing start, duplicate terminal suppression, failure, truncation and replay exclusion. Existing correlation is retained when available; no protocol or extension was added, and bounded writer overflow becomes a visible recording error.
- [x] 2.4 Enrich native resume projections by matching trace identity and call IDs, including page-split calls and folded file groups. Verify saved metrics match live activity rendering, create rows still suppress their existing metrics, no output is synthesized, old trace-less calls show unknown duration, and missing/late trace results cannot duplicate executions or contaminate a replacement session. Both recorders load matched metrics at attachment before native Snapshot/History reduction. Projection tests cover result-before-call pages, trace-less unknown duration, output preservation, and folded groups using their trace span.

## 3. Command completion and history actions

- [x] 3.1 Extend the central command declarations with fixed subcommand descriptors/default dispatch; reuse current candidate/description/ranking helpers rather than undoing the active completion refactor. Register history's four actions. Verify whitespace, partial completion, default show, invalid extra arguments, explicit-submit-only behavior, localization, atomic-block suppression, and unchanged dynamic model/effort completion and provider forwarding. The shared catalog now owns `show`, `path`, `copy`, and `copy-10`, with localized descriptions and completion tests; `/history` defaults to `show` and malformed extras remain local usage errors.
- [x] 3.2 Wire session-scoped history queries through runtime ports and actions. Implement path insertion using guarded ordinary composer editing, full chronological text export and stable top-ten ranking over eligible measured operations, followed by existing clipboard feedback. Verify spaces/non-ASCII paths, edits made during a pending request, no automatic prompt/transcript insertion, deferred drafts, stale results, failed clipboard writes, long/full summaries, incomplete warnings, finite live snapshots, ties and fewer than ten eligible records. Shared query actions execute through adapter ports outside UI locks. Path results check session/cwd plus exact composer state; exports use finite watermarks, stable call ranking, warnings, and existing clipboard notices.

## 4. Full-screen page lifecycle and terminal history rendering

- [x] 4.0 Define fixed history base/operation/duration theme roles, configure every built-in theme, and derive missing history groups from the same user theme's existing semantics. Preserve strict validation of explicitly supplied groups and keep rank/outcome meanings separate. Validate schema acceptance/rejection and backward compatibility without adding visual-style tests; page-level default-background reset is delivered with rendering in 4.3. All six embedded themes and legacy custom-palette acceptance passed, as did explicit missing/unknown/unresolved-role rejection and existing theme parser tests.

- [x] 4.1 Add a closed full-screen page owner and Screen-level composition branch, independent of Input Pages and Reading. Preserve canonical conversation/draft/anchor/inactive-view state; keep backend reduction alive and restore without an obsolete store clone. Verify no-event enter/exit equality, atomic blocks, background completion, resize, session switch, incoming approval/question, paste/submission suppression and stale-load cancellation using scoped state/controller tests. `HistoryPage` is an exclusive Screen branch over canonical state; controller routing suppresses composition, closes for protected interactions/session changes, and rejects stale identity/request completions.
- [x] 4.2 Add a shared full-screen browse key scope to the declarative defaults/resolver with one-row, half/full-page and exit actions; route page wheel input and committed-screen selection without hidden transcript scrolling or separator capture. Verify remap/disable behavior, q/Esc only closing the page, effective hints, viewport clamping, tiny heights and existing Input Page/Reading bindings remaining unchanged. Declarative `full_screen` and `history` scopes drive routing and footer labels; focused tests cover remapping/disablement, and production PTY checks covered row movement, exit, resize, and restoration.
- [x] 4.3 Add a history-scoped remappable Tab toggle between grouped turns and a session-wide descending Top 50 call list, preserving independent view positions and shared scrollable overview/legend. Verify the 50-call limit across wrapped rows, ties, eligibility, empty results, rank colors and effective hints. Render the approved title-free time axis, operation legend, parallel lanes, short-operation markers and chronological metrics with existing theme roles and downward rendering layers. Provide loading/empty/error/incomplete states and bounded page reads/layout. Verify narrow/zero geometry, long/non-ASCII summaries, non-color status readability, overlap geometry, exact durations, independent scrolling and no hidden transcript rebuilds on stable page frames; compare a real Ferra PTY capture with the approved demo. Rendering tests cover geometry, clipping, fixed footer and terminal-default background. A real `pie` PTY at 120×36 and 72×22 exercised populated grouped/Top-50 views, independent offsets, Tab, j/k, resize and q restoration; the Ferra capture matched the approved layout.

## 5. Integrated delivery

- [x] 5.1 Update the README's public commands/keys and discoverable storage guidance, localized completion/help labels, effective binding reference and the client architecture's full-screen/persistence boundary. Document attached-client-only coverage and command-text sensitivity without mirroring private implementation details. Run focused adapter/runtime/completion/page/projection/architecture regressions, builds for both executables, workspace formatting and Clippy for this cross-cutting change; report unavailable PTY or runtime checks explicitly. README, key-mapping guidance, bilingual help and client architecture now document the public boundary. `cargo fmt --all --check`, complete `e-tui`/`e-pi`/`e-dsh` tests, both builds, architecture checks and package-scoped all-target Clippy passed; Clippy still reports the repository's existing non-denied warnings. A production `pie` PTY run passed.
- [x] 5.2 Validate this change strictly, confirm every implementation task and the user's visual gate are satisfied, then merge deltas and archive through the documented OpenSpec CLI. Preserve unrelated dirty-tree work and do not archive other active changes; validate the three resulting main capabilities before reporting production delivery complete. Strict change validation and all implementation gates passed; this completion record is followed immediately by the one documented CLI merge/archive operation and post-archive capability validation.
