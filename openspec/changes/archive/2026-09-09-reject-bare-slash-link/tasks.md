## 1. Implementation

- [x] 1.1 Reject the exact bare slash in shared classification and test prose, quotes, code, and Markdown destinations while preserving real paths and URLs; run `cargo test -p e-tui --lib link_copy::tests`.
