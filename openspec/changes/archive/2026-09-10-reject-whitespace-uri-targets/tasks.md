## 1. Implementation

- [x] 1.1 Reject whitespace-bearing URI candidates and cover commit subjects, Unicode whitespace, valid custom/encoded URIs, and paths containing spaces; run `cargo test -p e-tui --lib link_copy::tests`.
